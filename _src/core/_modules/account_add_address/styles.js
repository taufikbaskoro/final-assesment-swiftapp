import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 20,
    marginBottom: 10,
    alignSelf: 'center',
    width: '100%',
  },
});

export default styles;
